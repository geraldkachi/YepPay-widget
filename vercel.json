{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "buildCommand": "NODE_OPTIONS=--openssl-legacy-provider yarn install --legacy-peer-deps && NODE_OPTIONS=--openssl-legacy-provider yarn build"
}