{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
 "buildCommand": "NODE_OPTIONS=--openssl-legacy-provider GENERATE_SOURCEMAP=false npm run build",
  "outputDirectory": "build",
  "framework": "create-react-app"
}